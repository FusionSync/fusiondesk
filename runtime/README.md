# Runtime

Runtime composes sessions, policy, network, and modules.

Expected units:

```text
host
session
product
diagnostics
qt
```

Runtime may depend on core and adapters. Core must not depend on runtime.

