# Display Module

Display owns server-side screen capture and client-side rendering.

Initial layout:

```text
common
agent
client
platform/windows
platform/linux
platform/macos
platform/android
```

The first implementation target is primary-screen capture on Agent and rendering on Client.

