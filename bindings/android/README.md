# Android Binding

The Android binding provides a Java/Kotlin facade over the native controller runtime.

It should expose controller lifecycle, session connect/disconnect, render surface attachment, input injection, and diagnostics callbacks.

