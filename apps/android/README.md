# Android Apps

Android outputs:

```text
client_qt
controller_library
```

`client_qt` is the Qt Android controller application. `controller_library` is the embeddable controller library surface for native Android apps.

