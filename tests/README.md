# Tests

Test groups:

```text
core/network
core/module
core/policy
core/session
modules/display
modules/feature
runtime/feature
platform/windows
adapters/qt
apps/pc
android
```

The first test priority is routing, policy decisions, module ingress registration,
display first-frame flow, feature runtime pumping, and one high-level smoke per
new app/runtime frame.
